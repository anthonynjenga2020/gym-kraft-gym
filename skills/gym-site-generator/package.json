{
  "name": "jenga-gym-site-generator",
  "version": "1.0.0",
  "description": "Master pipeline: Supabase intake row → live gym website on Vercel. Chains config-builder, github-repo-scaffolder, and vercel-deploy.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "generate": "node index.js --id $ID",
    "dry-run":  "node index.js --id $ID --dry-run"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
